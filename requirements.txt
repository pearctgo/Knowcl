# ============================================================
# requirements.txt — 版本全部 pin，保证可复现
# 安装: pip install -r requirements.txt
# 注意: torch / dgl 的 CUDA 版本需按本机 GPU 驱动选择
#       访问 https://pytorch.org/get-started 选择对应命令
# ============================================================

# ── 深度学习核心 ───────────────────────────────────────────
# torch 和 torchvision 建议通过官网命令单独安装（含 CUDA 版本）
# CPU-only 版本（可直接 pip install）:
torch==2.1.2
torchvision==0.16.2

# ── 图神经网络 ─────────────────────────────────────────────
# DGL CPU-only 版本；GPU 版本见 https://www.dgl.ai/pages/start.html
dgl==2.1.0

# ── 视觉 backbone 统一入口 ─────────────────────────────────
timm==0.9.12

# ── 地理数据处理 ───────────────────────────────────────────
geopandas==0.14.3
rasterio==1.3.9
shapely==2.0.3
pyproj==3.6.1
fiona==1.9.5

# ── 表格与数值计算 ─────────────────────────────────────────
pandas==2.2.1
numpy==1.26.4
scipy==1.12.0

# ── 机器学习工具 ───────────────────────────────────────────
scikit-learn==1.4.1.post1

# ── 配置文件 ───────────────────────────────────────────────
PyYAML==6.0.1
python-dotenv==1.0.1

# ── 可视化 ─────────────────────────────────────────────────
matplotlib==3.8.3
seaborn==0.13.2
umap-learn==0.5.6

# ── 图像处理 ───────────────────────────────────────────────
Pillow==10.2.0
opencv-python-headless==4.9.0.80

# ── 进度条 / 日志 ──────────────────────────────────────────
tqdm==4.66.2
loguru==0.7.2

# ── 数据校验 ───────────────────────────────────────────────
jsonschema==4.21.1
